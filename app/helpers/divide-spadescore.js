import Ember from 'ember';
import SpadeConstants from '../utils/spade-constants';
export function divideSpadescore(params/*, hash*/) {

  let spadeBookValue = SpadeConstants.SPADE_BOOK_VALUE;

  return params/spadeBookValue;
}

export default Ember.Helper.helper(divideSpadescore);
