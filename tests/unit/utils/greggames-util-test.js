import greggamesUtil from 'greggames-ui/utils/greggames-util';
import { module, test } from 'qunit';

module('Unit | Utility | greggames util');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = greggamesUtil();
  assert.ok(result);
});
