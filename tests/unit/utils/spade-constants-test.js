import spadeConstants from 'greggames-ui/utils/spade-constants';
import { module, test } from 'qunit';

module('Unit | Utility | spade constants');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = spadeConstants();
  assert.ok(result);
});
